public class Converter {
    double setDistance(int steps) {
        double distance;
        distance = steps * 0.00075;
        return distance;
    }
    double setKkal(int steps) {
        double kkal;
        kkal = steps * 0.05;
        return kkal;
    }
}
